{
  "name": "firestore-rules-test",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-t \"node test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.0",
    "firebase": "^10.14.1",
    "firebase-tools": "^13.0.0"
  }
}

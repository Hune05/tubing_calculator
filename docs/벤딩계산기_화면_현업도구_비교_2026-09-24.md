# 벤딩 계산기 화면·흐름 — 현업 도구와 비교 (2026-09-24)

읽은 기준: `fc4dd31`. 코드를 읽기만 했고 폰에서 돌려 보지는 않았습니다.

(작성 중 — 첫 판)

## 2. 문제점 (첫 판)

1. 인치로 바꾸면 반경·게인·최소 직선·원점·피팅 깊이 칸이 인치 숫자로 바뀌어 저장되는데, 이름표는 [mm]이고 엔진·입력 탭은 mm로 쓴다.
   - `mobile_settings_tab.dart` 355-379(바꾸기), 204-231(저장), 1300(이름표 [mm]), `mobile_result_tabs.dart` 67-71(반경 그대로 씀)
2. 폰 설정 탭이 저장된 12.7mm를 목록에서 못 찾아 3.0mm로 바꾼다.
   - `mobile_settings_tab.dart` 81, 87, 118, 1112, `settings_controller.dart` 18-33

코드는 고치지 않았습니다.

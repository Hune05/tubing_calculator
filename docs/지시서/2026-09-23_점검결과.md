# 앱 전체 점검 결과 (2026-09-23 밤)

화면 폴더 22개(약 13만 줄)를 7묶음으로 나눠 읽고, 찾은 것 가운데 **고칠 수 있는 것은 고쳐서 push**했습니다.
고치지 않은 것은 아래 "아침에 정할 것"에 까닭과 함께 적었습니다. 전체 테스트 1545개 통과.

## 고친 것 (커밋별)

| 커밋 | 묶음 | 고친 것(굵은 것이 제일 중요) |
|---|---|---|
| 802a4a6 | C-5 일정·프로젝트 | **개인 일정 저장이 통신 없을 때 안 닫히고 두 번 저장되던 것**, **고칠 때 완료 표시가 지워지던 것**, **Hive→서버 옮기기가 통신 없을 때 다시 돌아 서버 것을 옛 자료로 덮을 수 있던 것**, 알림 다시 잡기가 한 건 때문에 다 멈추던 것, 홈 배지 0으로 나오던 것, 카카오 검색 8초, PC 프로젝트 화면 죽는 곳·두 번 닫기, 템플릿 줄 키·넘침, 달력 막대 글자 크게, 안 쓰는 옛 화면 2개 삭제 |
| bc2165c | C-2 재고·자재 | **재고조사에서 수량을 치면 최소 수량이 0으로 올라가 자재 부족 알림이 꺼지던 것**, **올리기가 통신 없을 때 멈추고 두 번 기록되던 것**, 뒤로 가기로 센 수량이 다 사라지던 것(묻기), 지난 재고조사 셈이 영영 누적되던 것, 태블릿 빼기·넣기 두 번 빠짐, 태블릿 재고 부족 기준을 폰과 같게, "null" 글자·영어 키, 스피너 6곳 오류 표시, 자재 목록 채우기가 통신 없을 때 영영 돌던 것 |
| ae4d6e9 | C-3 일지·주간 보고 | **폰 뒤로 가기로 나가면 이슈 처리·일지·일정 고친 것이 사라지던 것(3화면)**, **사진이 캐시에 있어 올리기 전에 사라질 수 있던 것**, 달력에서 고친 일지가 확정을 우회하고 일정·이슈 반영이 안 되던 것, 사진 지우기 묻기·일정 지우기 되돌리기, 저장 실패 알림, 없는 칸에서 죽던 것, 주간 보고 기한 초과 두 셈, 넘침 5곳, 영어 글, 죽은 코드 |
| f508ffe | C-7 설정·메뉴·시작 | **태블릿 설정에서 관 굵기가 3mm로 바뀌어 저장되던 것**, **톱날 손실이 저장할 때마다 0으로 지워지던 것**, **QR·딥링크가 로딩 화면 뒤로는 안 열리던 것**, 가로로 든 폰이 "로그인 필요"로 들어가던 것, 프로필 저장 통신 없을 때 멈춤, 알림·차량·전동 목록 죽는 곳, 날씨 줄 넘침, 통신 없을 때 빈 백업 파일을 만들던 것 |
| 962c06b | C-1 튜브·형강 컷팅 | **임시 저장을 읽기 전에 저장이 먼저 돌아 빈 화면이 덮던 것**, 저장 실패가 조용히 사라지던 곳(튜브 2·형강 15), 기록 지우기가 통신 없을 때 합계를 안 빼던 것, 잔재 저장 멈춤·두 번 저장, 톱날 손실 잘못 치면 0, 템플릿 저장 멈춤, 규격 필터 쪽 어긋남, 형강 분류 필터 막다른 길, 형강 삭제 때 변경 이력 남던 것 |
| 44b08b9 | C-6 계산기 화면 | **보관함 도면을 열기만 해도 시작 방향이 바뀌어 저장되던 것**, 전선관 설정 읽기 전 규격 바꾸면 저장값 덮던 것, 리모컨·보관함·전동 목록 죽는 곳, 튜브 전체 지우기 묻기, 줄 고칠 때 다른 칸 잃던 것, 전동 마킹 총 절단 길이·마지막 길이 셈이 다른 화면과 다르던 것, 22.5°가 23°로, 넘침 3곳 |
| 608fbd4·e4417ae | C-8 | analyze 경고 13건 정리, 받은 도면 창이 작은 폰에서 넘치던 것, 새 화면 넘침 테스트 9건 |

## 아침에 정할 것 → 09-23 아침에 같이 정함 (결정과 처리)

| 번호 | 결정 | 처리 |
|---|---|---|
| 1 | 그대로 둔다 | — |
| 2 | 새로 쓰는 것부터 연도 포함 | PC 일지에 `dateISO`를 같이 저장(고칠 때는 원래 것 유지) |
| 3 | 읽을 때 둘 다, 쓸 때 하나로 | 부족 판정·상세·목록이 `min_qty`·`size`도 읽고, 태블릿 등록은 `minQty`·`spec`으로 씀 |
| 4 | 기록마다 톱날 몫 저장 | `CutRecord.kerf` 추가, 지울 때 (절단 길이+톱날)×세트를 뺌. 예전 기록은 0 |
| 5 | 그대로 둔다 | — |
| 6 | 이름별 폴더 | `app_backups/my_projects/<이름>/`에 올리고, 목록은 내 폴더 + 예전 맨 위 폴더 |
| 7 | 옛 문서 내용을 새 문서로 복사 | 이름 바꿀 때 users/{옛 이름} 칸을 users/{새 이름}에 복사(옛 문서는 둠) |
| 8 | 배치도 손볼 때 같이 | 아직 |
| 9 | 모두 지운다 | 발주·발주 기록·채팅 3개·차량 관리자 화면과 그 테스트 삭제 |
| 10 | 배치도 PDF만 고친다 | 한글 글꼴, 제목·판 이름·안내글 한글, 빈 캐비닛 측판은 안 찍음 |
| 11 | 그대로 둔다 | — |
| 12 | 알림을 누를 때 다시 읽어 만든다 | **다시 보니 이미 그렇게 되어 있음**(누를 때 `fetchAllProjects`로 읽음). 밤 점검이 잘못 본 것. 안 고침 |
| 13 | 달력에 둘 다 보이게 | 같은 날 일지가 여럿이면 날짜 칸에 개수, 누르면 고르는 창. 요약 복사·합계도 다 셈 |
| 14 | 다 지운다, 되돌리기 됨 | 모든 탭·경로·치수선을 지우고, 되돌리기 한 번에 전부 돌아옴(지운 탭에서) |
| 15 | 재단 계획 시트와 같게 | 잔재·섞어 쓰기 길이를 넣어 셈. "잔재 N본 씀"도 같이 적음 |
| 16 | pubspec 버전을 읽어 보인다 | `package_info_plus`로 설치된 버전 표시 |
| 17 | 주간 보고가 만든 파일만 지운다 | 보고서 PDF를 `temp/report_pdf/`에 두고 정리도 그 폴더만. 저장 공간 화면 셈에도 포함 |

폰 확인(09-23 아침, 커밋 6f378ec): 시작 화면 v1.0.0 표시(16), 새 캐비닛 임시 도면에서 전체 지우기 → 되돌리기로 모듈 3개·치수선 1개 복귀(14). 서버에 쓰는 3·4·6·7·10(PDF 공유)은 폰에서 안 눌렀고 테스트·분석으로만 확인. 설정 파일은 되돌림(md5 같음).

아래는 밤에 적어 둔 원래 글입니다.

**자료 구조·서버가 바뀌는 것 (지시서 규칙대로 손대지 않음)**
1. 프로젝트 저장(`upsertProject`)이 문서를 통째로 덮어 씁니다. 다른 폰에서 그 사이 넣은 일지가 지워질 수 있습니다. 바꾸려면 칸별 update나 트랜잭션으로 구조를 바꿔야 합니다.
2. PC 프로젝트 화면의 작업 일지 날짜가 "MM/DD"(연도 없음)로 저장됩니다. 해가 바뀌면 구분이 안 됩니다.
3. 재고 자재의 최소 수량 칸 이름이 두 가지(`minQty`·`min_qty`), 규격도 두 가지(`spec`·`size`)입니다. 태블릿 신규 등록에서 넣은 최소 수량은 폰에서 안 보입니다. 하나로 합치려면 서버 자료를 옮겨야 합니다.
4. 튜브 컷팅 누적 합계에 톱날 손실이 들어가는데 기록을 지울 때는 안 빠집니다. 기록마다 톱날 손실 몫을 저장해야 맞습니다.
5. 형강 컷팅 상세는 항목 배열을 통째로 저장합니다. 두 폰이 같은 작업을 열면 서로 덮습니다.
6. 클라우드 백업 폴더(`app_backups/my_projects`)가 사용자 구분이 없어, 두 사람이 쓰면 서로의 백업을 지웁니다. 폴더에 사용자 이름을 넣으면 예전 백업이 목록에서 사라집니다.
7. 프로필 이름을 바꾸면 사진·알림 토큰이 옛 이름 문서에 남습니다(사용자 문서가 이름으로 되어 있어서).
8. 배치도: 서버 저장이 `merge`라 경로를 다 지우고 저장해도 서버에는 옛 경로가 남습니다(임시 저장은 괜찮음). 배경 사진 경로는 폰 안 경로라 다른 폰에서는 안 보입니다. → 다음에 배치도 손볼 때 같이(아래 "다음에 고칠 것").

**사용자 결정이 필요한 것**
9. 발주(`material_order_page`)·발주 기록·채팅·차량 관리자 화면은 어디서도 열리지 않습니다(9/20에 메뉴에서 뺐다고 적혀 있음). 지울지, 다시 메뉴에 넣을지.
10. 영어 글: 태블릿 계산기 화면들("MARKING GUIDE", "BENDING WORKSPACE", "TUBE FABRICATION REPORT", "PROJECTS", "ONGOING/COMPLETED", "As-Built"), 홈 메뉴 배지("Smart/Main/New"), 시작 화면("FIELD HELPER", "TAP TO START"), 배치도 PDF(영어 제목·한글 글꼴 없음). 전에 "옛 프로젝트 화면 영어는 결정 대기"라고 하셨던 것과 같은 묶음입니다.
11. 홈 배지의 "오늘 N건"은 안 끝낸 것만 세고, 내 일정 화면 머리글은 다 셉니다. 어느 쪽으로 맞출지.
12. 주간 보고 자동 PDF(금요일 알림)는 알림이 만들어질 때 자료로 만듭니다. 그 뒤 쓴 일지는 안 들어갑니다.
13. 일지 달력은 같은 날짜 일지가 둘이면 하나만 보입니다(막을지, 둘 다 보일지).
14. 배치도 "도면 전체 지우기"가 지금 탭만 지웁니다(경로·다른 탭은 남음). 이름을 "이 탭 지우기"로 바꿀지, 다 지울지.
15. 형강 결과 탭 머리의 "새 원자재 N본"은 잔재·섞어 쓰기를 빼고 셉니다(재단 계획 시트와 다름). 같게 맞출지.
16. 시작 화면 "v2.0" 글은 pubspec(1.0.0)과 다릅니다. 지울지, 맞출지.
17. `temp` 정리가 모든 .pdf를 지웁니다(테스트가 그렇게 고정해 둠). 다른 기능이 임시 폴더에 둔 PDF까지 지울 수 있습니다.

**다음에 고칠 것 (배치도, 밤에는 손대지 않음 — 이번 주 새로 만든 곳이라 낮에 같이 보는 게 나음)**
18. 경로 고치기가 되돌리기 기록에 안 들어가 되돌리기가 경로를 옛것으로 되돌립니다.
19. 여러 개 선택 복제·가져오기가 뒤집기·잠금을 안 옮깁니다.
20. 평면 부품을 지우면 정면·측면의 그 부품 치수(view_…)가 허공에 남습니다.
21. 자재 수량이 지금 탭 부품만 셉니다(측판·평면 빠짐).
22. 스키드 크기를 바꿔도 정면·측면 판 폭이 안 따라옵니다.
23. 판 크기에 0을 넣으면 화면이 깨집니다(NaN).
24. 정면·측면에서 경로 손잡이가 부품 위에 있어 부품을 잡으려다 경로가 열립니다.
25. 공유로 받은 도면 파일이 앱 폴더에 계속 쌓입니다(지우는 곳 없음).
26. 새 부품 아이디가 밀리초라 같은 순간 둘이 생기면 키가 겹칩니다.

**폰에서 확인 못 한 것**
- C-1~C-7 고침은 테스트·분석으로만 확인했습니다(통신 없는 상황·서버 쓰기는 폰 실기가 필요). 아침에 다음 셋만 눌러 보시면 됩니다: ① 재고조사에서 수량 치고 올리기 → 자재 부족 표시 그대로인지, ② 개인 일정 하나 고치기 → 완료 표시가 남는지, ③ QR 배치도 링크가 홈 뒤에도 열리는지.
- 진짜 프로젝트(루마·H2·전선관·잔재)는 밤새 폰에서 건드리지 않았습니다(A·B 확인 때 임시 저장만 쓰고 되돌림).

# 할 일 메모 (다음에 이어서 할 것)

Claude 세션은 끝나면 기억이 사라지므로, 이어서 할 일을 여기에 적어 둔다. 새 세션에서 "docs/할일_메모.md 보고 이어서 해줘"라고 하면 된다.

## 확인이 필요한 것
- [ ] **Firebase 콘솔에서 익명 로그인이 켜져 있는지** (Authentication → 로그인 방법 → 익명). 본가에 있어서 아직 확인 못 함. 예전에 켠 적이 있을 수 있음.
  - 켜져 있으면: 새 앱을 깔고 한 번 연 뒤 `firestore.rules`를 콘솔 → Firestore → 규칙에 올린다.
  - 꺼져 있으면: 켜고 위와 같이. 켤 수 없으면 규칙은 올리지 말 것(구글 로그인 안 한 사람이 서버를 못 씀).
- [x] 컴파일·테스트: 2026-09-24 클라우드에서 Flutter 3.41.9로 `flutter analyze`(오류 0)·`flutter test`(1,666개 모두 통과) 확인. GitHub에도 자동 검사(`.github/workflows/flutter-test.yml`)를 넣어 올릴 때마다 돈다.
- [ ] 폰·PC에 새로 깔아 화면을 눌러 보는 확인(재고조사 올리기, 씀·채움, 내 것/공용 칩, 리모컨 보내기)은 아직.
- [ ] **수평계·각도기 폰 확인**(센서는 실제 폰에서만 확인 가능): ① 눕혀서 기포가 **높은 쪽**으로 가는지(한쪽 밑에 동전을 괴어 보기), ② 세워서·옆으로 세워서 기포관 방향, ③ 벤딩 각도: 90° 굽힌 관에서 90.0° 가까이 나오는지, ④ 나갔다 들어왔을 때 화면 꺼짐 방지가 설정대로 돌아오는지. 방향이 반대면 `level_page.dart`의 기포 부호 한 곳만 바꾸면 된다.

- [ ] **예전에 튜브 설정을 inch로 바꾼 적이 있으면** 반경·게인·피팅 깊이 값을 한 번 확인(점검 2번: 25.4로 나뉘어 저장됐을 수 있음. 새 코드는 더 망가뜨리지 않지만 이미 바뀐 값은 못 되돌림).
- [ ] 톱날 손실: 튜브 컷팅에서 한 번 확인(점검 14번. 예전에 형강에 맞춰 넣은 값이 튜브에도 남아 있음).

- [ ] **튜브 재고 빼는 곳이 바뀜**(점검 4번): 튜브는 재단 계획 창의 "재고에서 빼기"로. 목록의 "재고 차감"은 부속·예전 튜브 길이만. 폰에서 한 번 해 볼 것.

- [ ] **PC가 윈도우 앱이면** 새로 빌드해 켜지는지 확인(점검 24번, 클라우드에서는 윈도우 빌드를 못 해 봄).

- [ ] **서버 함수 배포**(점검 30·34번): `firebase deploy --only functions`. 발주·채팅 함수 3개를 지울지 물으면 지운다. 서버 알림이 "현장 중요 알림" 채널로 가고 누르면 작업 일지가 열린다.
- [ ] 폰에서 확인: 상태 표시줄이 보이고 화면이 안 겹치는지(35번), 새 일지 날짜 고르기(36번), 재단 계획 창 아래 "재단 설정 · 내보내기"(37번).

- [x] **UI·UX 점검 묶음 U-A**(`docs/UIUX_점검_2026-09-25.md`): X1·X2·X3·X5·X6·X7·X8·X9·X11·X12·F3 고침.
- [x] **UI·UX 점검 묶음 U-B**: U1·U2·U3·N8 고침.
- [x] **UI·UX 점검 묶음 U-C**: F1·F2·F4·F5·F6·X4·X10 고침.
- [x] **UI·UX 점검 묶음 U-D**: N1~N7·N9·U5·U6·F7 고침.
- [x] **UI·UX 점검 U4**: 설정 위 고정 영역을 스크롤 안으로. UI·UX 점검 제안은 모두 고침.
- [ ] **UI 디자인 산업용 비교 제안**(`docs/UI디자인_산업용비교_2026-09-25.md`): D-B(색의 뜻, 끝) → D-A(토큰·밝은 테마·글꼴, 끝) → D-C(공용 부품, 끝) → D-D(햇빛·야간 보기, 현장 화면만 끝) → D-E(머리·아이콘, 끝) → D-F(불러오는 중, 끝).

## 2026-09-26 PC에서 받아 폰 확인(1.1.0 디버그)
- analyze 오류 0, test 1,905 통과. 폰(폴더블 바깥 화면)에 깔아 확인:
  새 테마 홈, 재고 부족 배지, 튜브 마킹 +N 간격(150·419 → +269, 현장 탭 간격 합 = 절단 길이),
  현장 탭 닫으면 방향 고정 풀림(33번), 튜브 설정 반경·게인·깊이 mm 그대로, 현장 자료 검색·단위 환산,
  수평계 열림(기포 방향은 기울여 봐야 함 — 아직), 근태 화면. 서버는 users/{이름}에 uid만 더해짐.
- 폰에서 나온 것 고침: KEC·발전 설비·전선관 탭의 만드는 사람에게 하는 말(말투 막는 시험 추가),
  검색 결과 누르면 자판 닫기, 근태는 오늘 줄로 옮겨 열기, 현장 탭 세로 폭 첫 그리기 넘침(123px).
- 아직 폰에서 안 본 것: 재단 계획 "재고에서 빼기"·잔재(TEST 자재로), 재고조사·씀·채움·내 것/공용,
  리모컨(태블릿 필요), 사진 도장, 근태 저장, 배치도 전기 부품 회전.
- 튜브 설정이 inch인데 두께(WT)가 1.5로 저장돼 "1.5 inch"로 보인다(셈에는 안 쓰임, 예전 값).
- KEC 새 개정 알림(A)·요약 서버에 두기(B) 넣음 — `docs/KEC_개정_알림.md`. **남은 일(사용자)**:
  법제처 Open API 신청 → `firebase functions:secrets:set LAW_OC` → `firebase deploy --only functions`.
  폰에서 KEC 탭(앱에 든 요약·요약 기준 줄·출처) 확인함(2026-09-26 09:23). 새 공고 안내 상자는 서버 함수 배포 뒤에야
  폰에서 볼 수 있다(시험으로만 확인). 규칙 시험(에뮬레이터)은 이 PC에 Java가 없어 못 돌림.
- 현장 계산기(홈 "현장 작업"): 단위 환산·전기 계산기·압력 시험 계산기 폰 확인 끝(2026-09-26 11:30).
  압력 시험 "실제 시험 압력" 칸, **4-20mA 계산기**(환산·교정 점검·루프 전압, `docs/4-20mA계산기_근거.md`) 넣음.
  남은 후보: 전기 계산기 전선관 굵기·AWG, 4-20mA 상승·하강(히스테리시스) 점검·교정 기록 저장.

## 후순위로 미룬 것
- 앱 전체 점검 제안 목록 `docs/앱전체_점검_요약_2026-09-25.md`: 1~38번 모두 끝(3순위 25~29는 2026-09-25).
- [ ] **서버에 올리기**: `firestore.rules`를 콘솔에 붙여 넣기(먼저 `tool/firestore_rules_test`에서 `npm test`), `firebase deploy --only functions`(알림을 주인에게만). 익명 로그인이 켜져 있는지 먼저 확인.
- [ ] **배포 키**: `docs/배포_서명_버전.md` 순서로 키를 한 번 만들고 `android/key.properties`를 둔다. 새 키로 처음 깔 때 한 번은 지우고 다시 깔아야 한다.
- 리모컨: 오프셋·새들을 꺾임 여러 줄로 보낼지(현장에서 나머지 꺾임을 어떻게 넣는지 확인 먼저), R4 — `docs/리모컨_현업도구_비교_2026-09-24.md` 5절. 리모컨은 폰·태블릿 앱을 둘 다 새로 깔아야 서로 받는다.
- 자재관리 할 일 3(같은 이름·다른 제조사 차감 때 묻기), 5(엑셀 CSV 내보내기·가져오기), 6("발주 필요" 하나로) — `docs/자재관리_현업도구_비교_2026-09-23.md` 0절.
- 남의 개인 재고 읽기까지 서버에서 막기 — `docs/프로필_현업도구_비교_2026-09-24.md` 6절.
- 프로필 Q2·Q3 — 같은 문서 5절.
- 배치도 Q1(DXF 내보내기·튜브 경로·벤딩 반경·동시 수정 경고) — `docs/배치도_현업도구_비교_2026-09-23.md` 4절.

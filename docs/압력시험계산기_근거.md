# 압력 시험 계산기: 조항과 식의 근거 (2026-09-26 조사, 같은 날 보강·튜브 기준으로 다시 만듦)

홈 → 압력 시험(예전 이름 "압력 시험 계산기"). 탭(2026-09-26 순서): 시험 압력 · 시험 기록 · 압력 강하 · 공압 안전거리.
에어 누설 탭(DOE 구멍 누설·압축기 전력)은 2026-09-26 뺐다. 사용자 요청으로 계기 교정 "교정 가스" 탭(docs/교정가스_근거.md)으로 바꿨다.
시험 압력 탭 맨 위 "시험 대상"에서 **튜브(기본)** / 배관을 고른다. 튜브는 계기용 정밀 튜브(외경 기준: 인치 1/8"~1",
mm 6~25mm, SS316·탄소강)로, 허용 사용압력을 같이 확인하고 규격을 다른 탭(체적·수압 온도 영향·시험 기록)이 이어 쓴다.
배관은 전과 같다. 시험압력 계산은 둘이 같다("튜브 허용 사용압력" 절).
결과 화면에 조항 번호를 적는다. **최종은 해당 규격 원문과 발주처 절차서(ITP·시험 절차서)를 따른다.**
화면 글은 docs/계산기_용어_기준.md를 따른다.

## ASME B31.3 (공정 배관): 2018 본문, 2022·2024 바뀐 점
- 수압 345.4.2: PT = 1.5·P, 설계 온도 > 시험 온도면 × ST/S(재질 여럿이면 가장 작은 비). 시험압력은
  **배관의 모든 지점에서** 이 값 이상("at every point"). **2004판의 ST/S 6.5 상한은 현행판에 없다**: 계산에서
  제한하지 않고, 6.5를 초과하면 "이전 판은 6.5로 제한했으니 절차서를 확인하라"는 안내만 띄운다.
- 항복·부품 등급 초과 시 낮춤 345.2.1(a), 갇힌 물 팽창 345.2.1(b), 유지 10분 이상·설계압력 미만으로 안 내리고
  점검 345.2.2(a), 용기와 함께 345.4.3(77%).
- 공압 345.5: PT 1.1·P 이상, 1.33·P와 **345.2.1(a) 압력의 90%** 중 작은 것 이하(345.5.4 (a)(b), 2018 본문 확인).
  345.2.1(a) 압력은 관 응력(최소 두께 기준)이 항복강도가 되는 압력과 부품 등급의 1.5배 중 작은 것이라, 90%는 부품
  등급으로 1.35배다(09-26 고침: 전에는 "항복 기준 90%"만 적었다). 예비 점검 = ½PT와 170kPa 중 작은 것(345.5.5;
  2022판은 모든 이음). 안전밸브 설정압력 PT + min(345kPa, 10%)(345.5.2, 2022판 문구 바뀐 것은 못 봄).
- **공압을 고르는 근거는 345.1(b)**(발주처가 수압을 할 수 없다고 판단할 때 공압·수압-공압으로 대신). 전에는 345.5.1로
  적었다. 345.5.1은 저장 에너지·취성 파괴 주의 조항이라 주의 사항 줄에 따로 둔다(09-26 고침).
- **수압 전 예비 공기 시험 345.2.1(c)**: 큰 누설을 찾으려고 170kPa(25psi) 이하 공기 시험을 할 수 있다. B31.3 수압 주의
  사항에 "예비 점검(선택)"으로 넣었다(시험압력 값은 그대로).
- 압력계 2022판 345.2.2(d): 눈금 범위 약 2배(1.5~4배)는 **다이얼 압력계** 기준이고 디지털 압력계는 더 넓어도 된다.
  검교정 12개월 이내, **발주처가 승인하면 더 길게**(09-26 독립 검증 반영. 2022판 원문은 직접 못 봤고 Becht 2022 요약에
  "Leak Test Gauges (§345.2.2(d)) 요건 추가"만 확인). 압력계 카드와 수압 주의 사항에 적었다.
- 345.2.3(d)(2018 본문): 누설 시험을 마친 배관에 계기를 잇는 나사 이음·튜브 이음은 다시 누설 시험하지 않아도 된다.
  튜브 확인 사항에 넣었다.
- **2024판 345.2.3**: 누설 시험을 마친 기계적 이음부는 분해 후 다시 조립해도 재시험하지 않아도 된다. 이전 판은
  플랜지와 계기 연결 나사·튜브 이음만 해당. B31.3 두 시험 종류의 주의 사항 끝에 넣었다.
  출처: https://becht.com/becht-blog/entry/asme-b31-3-process-piping-changes-in-the-2024-edition/
- 화면 "실제 시험압력"(선택): 넣으면 안전밸브·예비 점검을 그 압력으로 계산하고 범위 밖이면 "최소 시험압력 미만"·
  "최대 시험압력 초과"로 빨갛게 알린다. 비우면 최소 시험압력으로 계산한다.
- 출처: B31.3-2018 6장 본문(pdfcoffee), Becht·pipingandinterface 2022·2024 변경 요약.

## ASME B31.1 (동력 배관): 2022 본문
- 수압 137.4.5: 1.5 × 설계압력 이상, 10분 이상 계속 유지, 설계압력으로 낮춰 점검. 응력은 항복의 90% 이하
  (137.1.4 → 102.3.3(b)). 보일러 외부 배관은 PG-99(137.3.1).
- **안전밸브 137.2.6**: 1⅓×PT 권장이지만 "137.1.4·137.4.5·137.5.5 한도를 넘지 않는 범위에서"다(2022 본문 확인).
  수압 결과 줄·주의 사항에 이 조건을 붙였다. **공압은 1⅓×PT가 늘 1.5P를 초과**(PT ≥ 1.2P → 1⅓PT ≥ 1.6P)하므로
  "안전밸브 설정압력: 1.5P(최대 시험압력) 이하, 격리하지 않은 기기 한도 이내" 줄을 새로 띄운다(09-26, 전에는 없었다).
- **137.4.6(d)는 수압 조항**: 매설·콘크리트 속·이중관처럼 육안 점검이 어려운 NBEP 이음부를 육안 점검에서 빼려면
  발주처 승인, 용접부 100% 체적 검사(RT·UT), 1시간 이상 유지, 압력·대기 온도 연속 기록이 모두 필요하고, 대기 변화로
  설명되지 않는 강하는 찾아 고쳐 다시 시험한다. 전에는 공압 압력 강하 탭에서 이 조항을 판정 근거처럼 적었다: 이제
  "판정 기준은 절차서가 정합니다"와 "이 조항은 수압·매설 이음부 조건"이라는 참고 줄로 바꿨다(09-26 고침).
- 공압 137.5: 1.2~1.5 × 설계압력, 10분 유지, 설계압력과 700kPa 중 작은 압력에서 점검(137.5.5).
- **예비 점검 137.5.4 (09-26 고침)**: 175kPa(25psig)를 넘지 않는 공압 시험을 큰 누설을 찾으려고 "할 수 있다"(may).
  선택이며 ½PT 규칙은 이 조항에 없다. 전에는 min(175kPa, ½PT)로 계산했다: 이제 "예비 점검(선택): 175kPa 이하".
- **단계 압력 137.5.5**: ½PT 이하까지 올린 뒤 약 PT/10씩 PT까지. 화면에 ½PT, 0.6PT … PT 표를 보인다.
- 출처: B31.1-2022 본문(energy-steel.com PDF), 137.5.4·137.5.5 문구 확인 https://www.wermac.org/others/ndt_pressure_testing.html .
  2024판은 못 봄.

## 물 기둥(최고점 높이), 압력계 눈금
- 수압에서 압력계보다 h(m) 높은 최고점 압력 = 압력계 압력 − 9.81kPa × h(물 1000kg/m³ × 9.81m/s²). 모든 지점이
  최소 시험압력 이상이어야 하므로(B31.3 345.4.2), 압력계에서 **최소 시험압력 + 9.81·h 이상**을 올려야 한다.
  칸 하나("압력계 위 최고점 높이")만 받는다: 압력계를 낮은 곳에 두는 보통 배치를 가정. 공압은 기체 무게가 작아 뺐다.
- 압력계 눈금: 시험압력의 약 2배(1.5~4배) 안의 EN 837-1 표준 눈금(bar) 0~0.6, 1, 1.6, 2.5, 4, 6, 10, 16, 25, 40,
  60, 100, 160, 250, 400, 600, 1000, 1600을 보이고 2배에 가장 가까운 것(비율로)을 알린다.
  출처: WIKA 자료 IN 00.02(2021-05) https://www.wika.com/media/Technical-information/English/ds_in0002_en_co.pdf

## 공압 안전거리: ASME PCC-2-2022 Article 501, 부록 501-II·501-III
- 저장 에너지 식 II-1: E = [1/(k−1)]·Pat·V·[1 − (Pa/Pat)^((k−1)/k)], Pa = 101kPa abs.
  공기·질소(k = 1.4)는 식 II-2 E = 2.5·Pat·V·[1 − (Pa/Pat)^0.286] **그대로** 쓴다(값이 전과 같다).
  헬륨·아르곤은 k = 1.67로 식 II-1. TNT[kg] = E / 4,266,920 (식 II-3).
  식 II-1 원문 확인: PCC-2-2008 부록 II PDF(chiefdelphi.com 사본), 2022판 k 형태는 LinkedIn 요약(Baher Elsheikh)과 같다.
- Pat는 앱이 게이지 + 101.325kPa로 더한다(PCC-2는 + 101kPa). 에너지가 조금 커져(독립 검증: 최대 약 3.2%, 낮은 압력에서)
  **안전 쪽이라 그대로 둔다**.
- 튜브면 관 내경·길이 칸 대신 "구간 1(시험 압력 탭 튜브) 길이"와 "구간 추가"(구간마다 규격·길이)로 체적 = Σ π/4·공칭 내경²·길이.
  체적 직접 입력이 있으면 그 값을 쓴다. 작은 체적도 보이게 1L 미만 소수 3자리, 10L 미만 2자리.
- **거리 (09-26 고침)**: 2018판부터 식 III-1이 R = Rscaled·**(2·TNT)**^(1/3)(지면 반사로 2배), Rscaled 20m/kg^(1/3)
  이상. 전에는 2008판 R = 20·TNT^(1/3)을 썼다(거리가 약 21% 짧았다).
- 최소 거리는 현행판에도 그대로 있다(501-III-1(a)): E ≤ 135.5MJ면 30m, 135.5MJ < E ≤ 271MJ면 60m. 이 최소 거리와
  식 거리 중 큰 것을 쓴다. 271MJ 초과는 식 거리를 쓰고 방호벽·시험 구간 분할을 검토하라고 알린다.
- **파편 거리(표 501-III-2-1)는 계산하지 않는다**: 화면에 빨간 글로 "파편 거리는 계산하지 않았습니다. 실제 출입
  통제 거리는 더 길 수 있습니다."를 띄운다. 2018판의 8D 체적 제한(단일 파손 해석)도 넣지 않았다.
- 확인한 곳(모두 2022판 기준):
  - https://www.piping-world.com/safe-distance-and-stored-energy-calculator-pneumatic-test (R = Rscaled × (2 × TNT)^(1/3), k = 1.4)
  - https://www.piping-world.com/pneumatic-testing-safe-distances-from-stored-energy-calculations
    (30m·60m 최소 거리, 예: 24" STD 600m 10barg → 224,057,795J, TNT 52.51kg, 94.36m. 계산기 값 223.998MJ·94.35m로 맞음)
  - https://humainengineering.com/tools/pneumatic-pressure-test.html (PCC-2-2022 Article 501, 최소 30m, 파편 표 501-III-2-1)
- 원문(ASME 유료)은 직접 못 봤다. 위 2차 자료 셋과 2008판 원문이 서로 맞는 것만 넣었다.
- ABSA AB-522 예(2172kPa, 0.5m³ → 1677kJ)와 에너지는 그대로 같다. 거리는 18.5m(전 14.7m), 최소 30m가 적용된다.

## 질소 용기·물 양 (보조 계산)
- 질소 필요량 ≈ 시험 구간 체적 × 절대 시험압력 ÷ 대기압(Nm³). 온도 영향은 빼고, 배관 안 공기까지 바꾼다고 본 값.
- 용기: 47L·150bar(가득 차면 약 7Nm³). 용기는 시험압력까지만 비울 수 있다고 보고 1병 = 47L × (150bar − 시험압력) ÷ 대기압.
  시험압력이 150bar 이상이면 용기만으로 못 채운다고 알린다. 병 수는 올림.
- 수압 시험 물 양: 체적(L) ≈ 무게(kg).

## 압력강하(공압) 누설률
- 이상기체: Q = V/Δt · T₁ · (P₁/T₁ − P₂/T₂) [Pa·m³/s], 절대압·절대 온도. BPVC V 10장 부록 VI에는 식·판정 기준이
  없다(부르는 규격이 정한다). 1 Pa·m³/s = 9.8692 atm·cm³/s(SE-1316), 1 mbar·L/s = 0.1 Pa·m³/s.
- **mL/min 기준 (09-26 고침)**: 전에는 시작 온도 값에 "0°C·1기압"이라고 적었다. 이제 20°C·1기압(293.15K,
  101.325kPa)으로 환산해 적는다: mL/min = Q × 293.15/T₁ × 9.8692 × 60.
  근거: Cincinnati Test Systems https://www.cincinnati-test.com/leak-rate-units (NDT Handbook 표준 293K·14.696psia).
- **판정 (09-26 고침)**: 근거 없던 빨간 기준(시작 절대압의 0.5%)을 뺐다. "허용 압력강하"(선택, 절차서 값)를 넣을 때만
  온도를 보정한 강하로 합격/불합격을 판정한다. 안 넣으면 판정 없이 보통 색. 압력이 오르면 "상승"으로 적는다.
- 온도 칸은 음수(영하)를 넣을 수 있다.

## 수압: 물 온도 1°C당 압력
- dP/dT = (β − 3α) / (κ + D/(t·E)·(5/4 − ν)), D = 평균 지름: 공기 없음, 축 방향 자유.
- **물 성질 (09-26 고침)**: 전에는 Kell(1975) 표 III(5~50°C) 8점을 직선 보간했다(β가 최대 2.9%, dP/dT가 부호가 바뀌는
  6~8°C 근처에서 최대 7% 어긋남, 독립 검증). 이제 Kell(1975, J. Chem. Eng. Data 20(1)) **닫힌 식**을 쓴다.
  ρ(t) = N(t)/(1 + 16.879850e-3·t), N = 999.83952 + 16.945176t − 7.9870401e-3t² − 46.170461e-6t³ + 105.56302e-9t⁴
  − 280.54253e-12t⁵. β = −(1/ρ)dρ/dt를 해석적으로 미분. κ = (50.88496 + 0.6163813t + 1.459187e-3t² + 20.08438e-6t³
  − 58.47727e-9t⁴ + 410.4110e-12t⁵)/(1 + 19.67348e-3t) × 1e-6 /bar. 범위 **0~100°C**(κ 식 범위). 밖이면 끝 값(0°C·100°C)으로
  계산하고 알린다. 5·10·20·30·50°C에서 표 III 값과 같음을 시험으로 확인(β ±0.02e-6, κ ±0.01e-6).
- 20°C 강관 D/t 20 → 약 3.1bar/°C, 6°C 근처에서 방향이 바뀐다.
- **매설(축 구속) 스위치 (09-26 추가)**: 매설·양 끝 고정으로 축 방향으로 늘지 못하면 dP/dT = (β − 2α)/(κ + D(1 − ν²)/(E·t)),
  D = 외경(API RP 1110·Technical Toolbox 식, 독립 검증 모델과 같음). 강관 D/t 20에서 약 7%, 두꺼운 튜브는 약 10% 크다.
- 재질 상수: 탄소강 α 11.7e-6/K·E 200GPa, 스테인리스 α 16.5e-6/K·E 193GPa. 316 자료 확인: UPMET 316/316L 자료
  20~100°C 16.5e-6·200GPa, Sandmeyer 316/316L 자료 20~100°C 16.6e-6·200GPa. E 193~200GPa 차이는 dP/dT 약 1~2%라 그대로 둔다
  (탄소강 α 11.5·E 203GPa로 바꿔도 0.6%, 독립 검증).
- 튜브면 외경·두께·재질 칸 대신 시험 압력 탭 튜브(공칭 치수, SS316 → 스테인리스, 탄소강 → 탄소강)를 쓴다.
- 결과는 고른 압력 단위로 보인다.

## 화면 편의
- 압력 단위 칩은 모든 탭이 같이 쓴다. 단위를 바꾸면 모든 탭에 넣어 둔 압력(설계·실제 시험·시작·종료·허용 강하·
  공압 시험·공급)을 새 단위로 환산해 뜻이 바뀌지 않는다(예: 10bar → 145.04psi). 다시 돌아오면 원래 값.
- "공압 안전거리" 탭의 "시험 압력 탭 값 가져오기": 공압이고 실제 시험압력을 넣었으면 그 값, 아니면 같은 규격의
  공압 최대 시험압력(안전 쪽).
- 넣은 값은 기기에 임시 저장(SharedPreferences 'pressure_test_draft_v1')해 다음에 열 때 되살린다.

## 튜브 허용 사용압력 (2026-09-26 추가)
코드: `tube_rating.dart`(계산, 화면 없음). 화면은 `pressure_test_page.dart`의 `_tubeInputs`·`_tubeResult`·`_tubeNotes`.

**입력**: 튜브 재질(SS316: ASTM A269·A213 이음매 없는 관 / 탄소강: ASTM A179), 치수 단위(인치 / mm), 튜브 규격(외경 × 두께),
설계 온도(°C, 선택. 비우면 38°C 이하).
- 인치 23개: 1/8"(0.028·0.035), 1/4"(0.028·0.035·0.049·0.065), 3/8"(0.035·0.049·0.065), 1/2"(0.035·0.049·0.065·0.083),
  3/4"(0.049·0.065·0.083·0.095·0.109), 1"(0.065·0.083·0.095·0.109·0.120). 모두 Swagelok 표 3(SS)·표 1(탄소강)에 있는 두께.
- mm 17개: 6×1, 6×1.5, 8×1, 8×1.5, 10×1, 10×1.5, 12×1, 12×1.5, 12×2, 14×2, 16×1.5, 16×2, 18×2, 20×2, 22×2, 25×2, 25×2.5.
  모두 Swagelok 표 4에 있는 두께. 기본: 인치 1/4" × 0.035", mm 12 × 1.5.
- 3/8" × 0.083"(SS 7500psig)과 16 × 2.5mm(400bar)는 Swagelok 표에서 피팅 반복 시험(4:1) 값이라 넣지 않았다.

**계산값**: B31.3-2018 304.1.2(a) 식 (3a)를 P로 푼 P = 2·S·E·W·t/(D − 2·Y·t), E = W = 1, Y = 0.4(표 304.1.1).
- **최대 외경·최소 두께로 계산**한다(Swagelok 3쪽 "maximum OD and minimum wall"과 같은 방법).
  - SS316: ASTM A269-14 표 4. 외경 ±0.005"(0.13mm), 두께 외경 1/2"(12.7mm) 미만 ±15%, 1/2" 이상 ±10%(원문 PDF 확인).
    mm 튜브도 A269 허용차로 계산한다(Swagelok mm 표는 EN ISO 1127 D4·T4(−7.5%)·T3(−10%) 기준이라 값이 더 크다).
  - 탄소강 A179: "최소 두께" 관이라 적힌 두께를 그대로 쓴다(Swagelok 표 1도 두께를 빼지 않음). 외경은 SS와 같게 +0.13mm로
    봤다(Swagelok 계산은 +0.004"로 맞는다. A450 원문은 못 봤다: 0.005"는 조금 보수적).
  - 화면에 "공칭 두께로 계산하면 X (참고)"도 같이 보인다.
- **t ≥ D/6이면 Y = d/(D + d)**(표 304.1.1 주, Lamé 식과 같다). 1/8" 튜브, 1/4" × 0.065" 등. 화면에 "304.1.2(b) 검토 대상"을
  적는다. Y = 0.4로 두면 1/8" × 0.035" SS가 약 11,220psi로 Swagelok 10,900보다 커져(안전하지 않은 쪽) 이렇게 했다.
- **S(B31.3-2018 표 A-1, 원문 사본 PDF에서 확인)**, 표 사이는 °F로 직선 보간:

| °F | ≤100 | 200 | 300 | 400 | 500 | 600 | 650 | 700 | 750 | 800 |
|---|---|---|---|---|---|---|---|---|---|---|
| SS316 A269·A213 TP316 (188·189쪽) | 20.0 | 20.0 | 20.0 | 19.3 | 18.0 | 17.0 | 16.6 | 16.3 | 16.1 | 15.9 |
| 탄소강 A179 K01200 (165·166쪽) | 15.7 | 15.7 | 15.3 | 14.8 | 14.1 | 13.3 | 12.8 | 12.4 | 10.7 | 9.2 |

  - SS316 두 번째 출처: Swagelok MS-01-107 표 26 온도 계수(316: 200°F 1.00, 400°F 0.96, 600°F 0.85, 800°F 0.80)가 위 S의 비와 같다.
  - A179 400°F 이상: 2018 PDF 추출 글에서 400°F부터 시작하는 계속 쪽(머리 "400 500 600 650 …")의 A179 줄과
    engineersforengineers.wordpress.com "ASME B31.3 Table A-1 … Carbon Steel Pipe and Tube"(2019) 표가 13칸 모두 같다.
    독립 검증은 300°F까지만 확인했었다: 이번에 두 출처로 800°F까지 확인해 넣었다.
  - 최저 온도: SS316 −254°C(−425°F), A179 −29°C(−20°F). 최고 427°C(800°F)까지만 넣었다. 밖이면 "표 A-1에 넣은 범위 밖이라
    계산하지 않습니다"로 막는다(값을 보이지 않음). 427°C 초과(크리프 영역)는 넣지 않았다.
  - 최소 항복강도(같은 표): SS316 30ksi(인장 75), A179 26ksi(인장 47).

**제조사 값**: Swagelok "Tubing Data" MS-01-107 **Rev W(2023-10)** 원문 PDF를 열어 확인했다.
- 표 1(3쪽) 인치 탄소강 A179, S = 15,700psi. 표 3(5쪽) 인치 스테인리스 이음매 없는 관 A269, S = 20,000psi.
  표 4(6쪽) mm 스테인리스 EN ISO 1127, S = 138MPa. 모두 −20~100°F(−28~37°C) 값.
- 표 칸 배치가 글 추출에서 흐트러지므로, 같은 방법(최대 외경·최소 두께, B31.3 식, t ≥ D/6이면 Lamé)으로 다시 계산해
  100psi(인치)·10bar(mm) 아래로 버린 값이 표와 모두 같음을 확인했다(SS 인치 23개는 시험이 매번 확인한다).
- mm 탄소강 표 2는 EN 10305-1(S = 113MPa) 관 기준이라 A179와 재질이 달라 **넣지 않았다**(화면에 "제조사 값 없음").
- Swagelok 표 머리말: 참고용이며 설계에 그대로 쓸 수 있다는 뜻이 아니다. 튜브 확인 사항에 적었다.

**허용 사용압력 = 계산값과 제조사 값 중 작은 것.** 제조사 값은 −28~37°C 값이라 설계 온도가 이 범위(−29·38°C까지 봄)일 때만
비교한다. 그 밖이면 "이 설계 온도에서는 계산값만 씁니다"로 적는다(Swagelok 온도 계수를 곱하지 않고 S를 보간한 계산값을 쓴다.
SS316은 결과가 같고, 탄소강 계수는 표 26에서 확인하지 못했다).
예: 1/4" × 0.035" SS316 → 계산 5,147psi, 제조사 5,100psig → 351.63bar. 12 × 1.5mm SS316 → 계산 316.5bar(A269 −15%) < 제조사 330bar.

**확인 줄**:
- 설계압력이 허용 사용압력을 넘으면 빨갛게 "허용 사용압력 초과".
- B31.3 수압: 시험압력(실제 값, 없으면 최소)이 튜브 항복 압력(최대 외경·최소 두께, 최소 항복강도로 같은 식)을 넘으면 빨갛게
  "항복 압력 이하로 낮출 수 있습니다(345.2.1(a))". B31.3 공압: "공압 최대 시험압력 = 1.33P와 항복 압력의 90% 중 작은 것"을
  보이고 넘으면 빨갛게(345.5.4). B31.1: 항복의 90%(137.1.4 → 102.3.3(b)).
- B31.3 수압이고 설계 온도 S가 시험 온도 S보다 작으면 ST/S(예: 300°C SS316 → 20 ÷ 17.28 = 1.157)를 보이고 "ST/S 칸에 넣기"
  단추를 띄운다. 시험 온도는 38°C 이하로 본다.
- B31.1이면 아래 "B31.1 튜브" 절처럼 B31.1 식·표로 계산한다(예전의 "B31.1 허용 응력으로 다시 확인하십시오" 줄은 뺐다).
- 튜브 확인 사항: 계통 허용 압력 = 튜브·피팅·밸브 중 가장 낮은 것(피팅·밸브 제조사 등급 확인), 부품 등급 1.5배(공압 1.35배),
  345.2.3(d) 계기 연결 이음 재시험 면제, 제조사 값은 참고.
- 시험압력 계산(절차·압력계·단계 압력 등)은 튜브·배관이 같다(바꾸지 않음).

**다른 탭으로 이어짐**: 공압 안전거리(구간 체적), 압력 강하 수압(튜브 치수·재질), 시험 기록(시험 정보에 튜브 줄, 수압이면
튜브 치수로 물 온도 영향, 기록에 tubeId·tube(규격 글)·tubeMat 저장, 기록서 PDF 시험 정보 왼쪽 칸 "튜브", CSV "튜브 규격" 칸).
기록을 불러오면 튜브 기록은 튜브·재질·규격까지, 이전 기록·배관 기록은 배관으로 되살린다.

**임시 저장**: tube(튜브/배관)·tubeMat·tubeSys·tubeId·restrained·segs(구간 규격·길이), 칸 tubeTemp·tubeLen.
'tube'가 없는 이전 임시 저장은 배관 기준으로 넣은 값이라 배관으로 되살린다. 새로 열면 튜브.

**B31.1 튜브 (09-26 추가)**: 시험 압력 탭에서 B31.1을 고르면 허용 사용압력을 B31.1로 계산한다.
원문: B31.1-2022 PDF(energy-steel.com, 위 "ASME B31.1" 절과 같은 사본). 글 추출(pdftotext)로 읽었다.
- 식: 104.1.2(a) 식 (9) **P = 2SEW(tm − A) / (Do − 2y(tm − A))**(27쪽). E = 1(A213·A179 이음매 없는 관, 표의 E 칸 1.00),
  W = 1(102.4.7: 이음매 없는 관이고 427°C 이하. 표 102.4.7-1 주 (7) 탄소강 관·튜브는 제외), **A = 0**(압축 이음 튜브는
  나사·홈을 내지 않음. 부식 여유를 정했으면 따로 빼야 한다).
- y: 표 104.1.2-1(28쪽) 482°C 이하 페라이트·오스테나이트강 0.4. 같은 표 일반 주 (b): **Do/tm < 6이면 y = d/(d + Do)**.
  B31.3(t ≥ D/6)과 경계만 다르다(정확히 6이면 B31.1은 0.4). 두꺼운 벽 식은 B31.3의 Lamé 값과 같다.
- 치수: B31.3과 같게 최대 외경·최소 두께. 104.1.2의 Do는 "규격 표의 외경"이라 공칭 외경을 써도 되지만, 최대 외경이 조금 낮게
  나와(안전 쪽) 한 방법으로 맞췄다.
- S(°F 이하 칸, 표 사이 직선 보간, 800°F = 427°C까지):

| °F | ≤100 | 200 | 300 | 400 | 500 | 600 | 650 | 700 | 750 | 800 |
|---|---|---|---|---|---|---|---|---|---|---|
| 표 A-3 A213 TP316 S31600, 주 (10) 줄(152·153쪽) | 20.0 | 17.3 | 15.6 | 14.3 | 13.3 | 12.6 | 12.3 | 12.1 | 11.9 | 11.8 |
| (쓰지 않음) 같은 규격 주 (9)(10) 줄 | 20.0 | 20.0 | 20.0 | 19.3 | 18.0 | 17.0 | 16.6 | 16.3 | 16.1 | 15.9 |
| 표 A-1 A179, 주 (1)(2)(5)(126·127쪽) | 13.4 | 13.4 | 13.4 | 13.4 | 13.4 | 13.3 | 12.8 | 12.4 | 10.7 | 9.2 |

  - 주 (9): 항복의 67%를 넘고 90% 이하인 높은 값. "조금만 변형돼도 새거나 오동작하는 곳"에는 쓰지 말라고 적혀 있어
    압축 이음 튜브에는 낮은 줄(주 (10))을 쓴다. 38°C 이하는 두 줄 모두 20.0이라 결과가 같고, 그 위에서 차이가 난다.
  - A269는 B31.1 필수 부록 A 표에 없다(부록 VIII 저온 표에만 나옴). 그래서 SS316은 같은 TP316인 A213 값을 쓰고 화면에 적는다.
  - A179 주 (1): 보일러 외부 배관(BEP)의 압력 부분에 쓸 수 없다. 화면에 적는다. 인장 (47)은 기대 최소값(주 (5)).
  - 행 맞춤 확인: 글 추출에서 TP304·TP309H 등 다른 강종도 같은 "주 (10) 줄 → 주 (9)(10) 줄" 순서였고, 주 (9) 줄 값이
    B31.3 표 A-1 TP316 값(앱의 B31.3 표)과 같다. A179 600~800°F 값(13.3·12.8·12.4·10.7·9.2)도 B31.3 값과 같다.
    13.4ksi는 인장 47ksi ÷ 3.5(B31.3은 ÷ 3 = 15.7)로, 예전 화면의 "B31.3 값의 0.85배"(Swagelok 표 1 주)와도 맞는다.
    두 번째 독립 출처(다른 사본)로는 대조하지 않았다.
- 최저 온도: 표 A-1·A-3에는 최저 온도 칸이 없고 124.1.2가 B31T(부록 VIII)로 따로 정한다. 표 VIII-1의 스테인리스 무리 저온
  사용 한계가 −20°F(−29°C)라 앱은 **두 재질 모두 −29~427°C**만 계산한다. 밖이면 "B31.1 표 A-3, A213 TP316에 넣은 범위
  (-29~427°C) 밖이라 계산하지 않습니다"와, −29°C 아래면 "124.1.2(B31T 요건)로 따로 확인" 줄.
- 제조사 값 비교는 B31.3과 같다(−28~37°C일 때 계산값과 작은 것). ST/S 줄은 B31.3 수압에만 보인다(그대로).
- 시험압력 한도(항복의 90%, 137.1.4 → 102.3.3(b))는 전과 같고, 항복 압력도 B31.1 식(두꺼운 벽 경계만 다름)으로 계산한다.
- 화면: 계산값 줄 "B31.1 104.1.2, S … ksi", 새 줄 "허용 응력 S: B31.1 표 A-3, A213 TP316, 설계 온도로 보간"(B31.3이면
  "B31.3 표 A-1, A269·A213 TP316"), 두꺼운 벽이면 "외경 ÷ 두께가 6 미만이라 y = d/(d + Do)로 계산했습니다(표 104.1.2-1 일반 주 (b))."
- 손 계산 예(시험에 넣음): 1/4" × 0.035" SS316 38°C 이하 → 1190 / 0.2312 = 5147.06psi, 제조사 5100psig가 정함(351.63bar).
  같은 튜브 200°F(S 17.3) → 4452.21psi. 100°C(S 17.096) → 4399.70psi = 303.35bar. 1/4" × 0.035" 탄소강 → 938 / 0.227 =
  4132.16psi = 284.9bar(제조사 4800보다 낮아 계산값. B31.3이면 4841psi라 제조사 4800이 정함). 1/8" × 0.035" SS316 →
  y = 0.0705/0.2005 = 0.3516, 10909.5psi.

**확인하지 못한 것**: A450 외경 허용차 원문, Swagelok 탄소강 온도 계수, 2022·2024판 표 A-1 바뀐 값
(2018 본문 기준), 구리·기타 합금(범위 밖), 용접 튜브(E·W가 1이 아님: 이음매 없는 관만).

## 2026-09-26 독립 검증 결과
규격 원문으로 따로 만든 기준 모델(ref.js)과 앱 계산(pressure_calc.dart를 그대로 돌린 app/run.dart)을 비교했다.
- 비교한 경우: 시험압력 13,440건(규격 2 × 시험 종류 2 × 3,360: 설계압력·ST/S·실제 시험압력 격자), 압력강하 18,000건,
  수압 온도 1,410건, 물 성질 121건, 저장 에너지 1,500건, 압력계 120건, 질소 120건, 에어 누설 216건 등 모두 34,948건
  (비교 항목 212,025개).
- **시험압력은 모두 정확히 같다**(최소·최대·유지시간·예비 점검·누설 확인 압력·안전밸브·단계 압력·범위 판정, 오차 0).
  압력강하·누설률·저장 에너지·TNT·거리·질소·압력계 눈금도 반올림 오차(1e-15) 안에서 같다.
- 고친 것(이 문서 각 절에 적음): B31.1 공압 안전밸브 줄 추가(137.2.6), 공압 근거 345.1(b)로 바로잡음(345.5.1은 주의 조항),
  345.5.4 "345.2.1(a) 압력의 90%·부품 1.35배", 137.2.6 한도 조건, 137.4.6(d)는 수압·발주처 승인·100% 체적 검사 조건(공압 탭
  글 고침), 345.2.1(c) 예비 공기 시험, 345.2.2(d) 다이얼·디지털·발주처 승인, 물 성질을 Kell 닫힌 식(0~100°C)으로,
  매설(축 구속) 스위치, PCC-2 Pa 101 vs 101.325는 안전 쪽이라 그대로 둠, DOE 1/4" 100psig는 100.9cfm.
- 튜브 허용 사용압력은 이 검증 뒤에 새로 넣은 것이라, 같은 방법으로 Swagelok 표를 다시 계산해 맞춰 봤다(위 절).

## 시험 기록 탭: 유지시간 타이머·측정 기록·판정·기록서 (2026-09-26 추가)
코드: `pressure_record_tab.dart`(탭 화면, pressure_test_page.dart의 part), `test_record.dart`(기록·판정·저장·CSV),
`test_record_sheet.dart`(저장 창), `test_record_pdf.dart`(기록서 PDF), `test_records_page.dart`(저장한 기록 목록),
`hold_alarm.dart`(완료 알림), `pressure_units.dart`(단위). 4-20mA 교정 기록(instrument/cal_record*)과 같은 틀이다.

**화면 순서**: 시험 정보(시험 압력 탭의 규격·시험 종류·설계압력·시험압력(실제 값이 있으면 그 값, 없으면 최소 시험압력)·
규정 유지시간) → 압력 단위 → 유지시간(분) → 라인 번호(선택) → 유지시간 타이머 → 측정 기록 → 허용 압력강하(선택)·
수압이면 관 외경·두께·재질(선택, 튜브면 시험 압력 탭 튜브 줄) → 누설·물맺힘 없음(육안 확인) → 판정 → 기록 저장·저장한 기록·새로 시작.

**유지시간**: 기본값은 시험 압력 탭의 규정 유지시간(B31.3·B31.1 모두 10분). 절차서가 30분·1시간 등을 정하면 그 값을
넣는다. 규격 최소보다 짧게 넣으면 빨간 글로 알리고 **규격 최소로 판정**한다(판정·타이머·알림 모두 둘 중 큰 값).

**타이머**:
- "시작"을 누르면 시작 압력·온도를 묻는다(압력은 앞 측정값, 없으면 시험압력으로 미리 채움. 온도는 마지막에 넣은 값).
  확인을 누른 시각이 시작 시각이다. 경과 시간(mm:ss, 1시간 넘으면 h:mm:ss)과 남은 시간을 1초마다 다시 그린다.
  유지시간이 지나면 "유지시간 완료"로 바뀌고 경과 시간은 계속 센다.
- 시작 시각·측정값·종료 시각은 임시 저장('pressure_test_draft_v1'의 'record': start·end·reads·leakOk·mat·editing·lastT,
  칸 값은 'fields'의 rHold·rAllow·rLine·rOd·rWall)에 바로 쓴다. 화면이나 앱을 나갔다 와도(앱이 꺼져도) 저장된 시작 시각으로
  경과 시간을 다시 계산한다.
- "측정 추가": 지금 시각으로 압력·온도 한 줄. 줄을 누르면 값을 고친다(시각은 그대로). 중간 측정 줄은 지울 수 있다.
- "종료": 종료 압력·온도를 기록하고 타이머를 멈춘다(측정 기록은 남는다). 유지시간 전에 누르면 창에 "지금 종료하면 불합격"을
  알린다. "새로 시작"(확인 창)은 시작 시각·측정 기록·누설 확인·라인 번호를 지운다(허용 압력강하·유지시간·관 치수는 남김).

**완료 알림**(`hold_alarm.dart`):
- 시작할 때 완료 시각(시작 + 유지시간)에 폰 알림을 예약한다. 번호 918400 하나, 채널 'pressure_test_hold'(압력 시험 알림).
  제목 "압력 시험 유지시간 완료", 본문 "{라인 번호} 유지시간 N분이 지났습니다. 종료 압력을 기록하십시오."
- 유지시간·라인 번호를 고치면 1초 뒤 다시 예약한다(같은 번호라 앞 예약이 바뀐다). 화면을 다시 열 때 아직 진행 중이면 같은
  알림을 다시 예약한다(완료 시각이 지났으면 건드리지 않는다).
- 종료·새로 시작·기록 불러오기 때 취소한다(이미 떠 있는 알림도 지워진다).
- 플러그인은 main.dart가 초기화한 것과 같은 하나(FlutterLocalNotificationsPlugin은 싱글턴)를 쓴다. 권한은 reminder_tools.dart와
  같은 규칙으로 앱 전체에서 처음 한 번만 묻는다(같은 키 'notif_permission_asked_v1'). 정확한 알람이 허용돼 있으면 정확한 시각,
  아니면 폰이 묶어 보내 **늦을 수 있다**(화면의 시간은 늘 정확하다). reminder_tools.dart를 가져오지 않은 까닭: 그 파일이
  main.dart를 가져와 계산기가 앱 전체에 묶이고, 계산기 시험도 앱 전체가 컴파일돼야 돈다.
- **정확한 알람이 꺼져 있을 때 (09-26 폰 시험 뒤 추가)**: 14:50:42 예약 알림이 14:56:11에 왔다(정확한 알람 권한이 없어
  안드로이드가 inexact로 약 +7.5분 창에 묶어 보냄). 이제 시작 전·진행 중(종료 전)에 canScheduleExactNotifications()가 false면
  타이머 아래에 빨간 줄 "정확한 알람이 꺼져 있어 알림이 몇 분 늦을 수 있습니다."와 "설정 열기" 단추를 띄운다. 단추는
  requestExactAlarmsPermission()(플러그인 21.0.0, reminder_tools.dart와 같은 호출을 hold_alarm.dart에 따로 둠)으로 폰 설정을 연다.
  돌아와서(단추 결과 또는 앱이 다시 앞으로 올 때 resumed) 켜져 있으면 진행 중인 시험의 알림을 정확한 방식으로 다시 예약한다.
  HoldAlarm.canExact()·requestExact()로 가짜를 넣어 시험한다(기본은 true: 플러그인이 없는 곳에서는 줄을 띄우지 않음).
  줄의 상태는 setState 대신 ValueNotifier로 다시 그려, 임시 저장을 읽기 전에 "사용자가 고침"으로 잡히지 않게 했다.
- 모든 플러그인 호출은 try/catch. 위젯 시험에서는 가짜(HoldAlarm)를 넣어 예약·취소를 확인한다.
- **알림을 누르면 시험 기록 탭으로 간다 (09-26 추가)**: 예약할 때 알림 글(payload) 'pressure_test_record'(`kPtHoldPayload`)를
  붙인다. main.dart `routeForNotification`이 이 글을 보면 `PressureTestPage(initialTab: kPtRecordTabIndex)`(탭 4 = 시험 기록)를
  여는 경로를 돌려준다. 앱이 꺼져 있을 때(getNotificationAppLaunchDetails)와 실행 중일 때(onDidReceiveNotificationResponse)
  모두 `_handleNotificationPayload`를 거친다. 실행 중이고 압력 시험 화면이 이미 열려 있으면 하나 더 열지 않는다
  (`PressureTestPage.revealOpen`: 그 화면 위에 열린 창·화면을 닫고 시험 기록 탭으로 옮김). 두 화면이 같은 임시 저장·같은 알림
  번호를 쓰므로 겹쳐 열리면 서로 덮어쓸 수 있어서다. 서버 알림 칸(data)의 'open' 값으로는 열지 않는다(폰 예약 알림만).
  시험: test/notification_open_test.dart(경로·탭 번호·이미 열린 화면). 폰에서 알림을 눌러 보는 확인은 아직 안 했다.

**판정**(`judgePressureTest`, 화면·기록서·CSV 같은 함수):
- 종료 압력이 있어야 판정한다. 시작 전·종료 전은 "판정 없음"과 그 이유.
- 공압: 시작·종료 온도가 둘 다 있으면 `pressureDecay`로 온도를 보정한 압력강하(종료 절대압 × T₁/T₂와 비교)로 판정한다.
  온도가 없으면 측정 강하로 판정하고 "온도 보정을 하지 않았습니다"라고 적는다.
- 수압: **측정 강하로 판정**한다(보정하지 않음). 관 외경·두께와 두 온도가 있으면 `hydroBarPerDegC`(평균 물 온도)로
  "온도만으로 압력이 약 X 낮아집니다/높아집니다(추정, 공기 없는 막힌 관 기준)"를 참고로만 보인다. 공기가 남으면 훨씬
  작아지는 추정 값이라 판정에 쓰지 않는다(B31.1 137.4.6(d): 대기 변화로 설명되지 않는 강하는 찾아 고친다).
- 합격 조건: ① 경과 시간(종료 − 시작) ≥ 유지시간 ② 허용 압력강하를 넣었으면 판정 강하 ≤ 허용값(안 넣으면 압력강하는
  판정하지 않음, 규격에 수치 기준이 없다) ③ "누설·물맺힘 없음(육안 확인)" 표시.
- ① 미만이거나 ② 초과면 **불합격**. ①②는 맞는데 ③을 표시하지 않았으면 **판정 없음**(확인하지 않은 것을 합격으로 적지
  않는다). 불합격·판정 없음이면 이유를 모두 적는다(예: "경과 시간 8분: 유지시간 10분 미만", "압력강하 0.1 bar: 허용
  압력강하 0.05 bar 초과", "누설·물맺힘 없음(육안 확인)을 확인하지 않았습니다.").

**서버 올리기**(2026-09-26, `lib/src/data/record_sync.dart`, 4-20mA 교정 기록과 같은 방식):
- 폰에 먼저 저장하고 서버(Firestore)에도 올린다. 화면은 늘 폰 저장을 읽으므로 통신이 없어도 열린다.
- 모음 `pressure_test_records`, 문서 이름 = 기록 id. 칸 = 기록 JSON + `owner`(앱 사용자 이름) + `ownerUid`(로그인 uid)
  + `editedAt`(저장한 폰 시각 ms) + `updatedAt`(서버 시각) + `deleted`(지움 표시).
- 저장·지우기 뒤 기다리지 않고 올린다(8초 넘으면 그만 기다림, Firestore가 폰에 쌓았다가 통신되면 올린다). 실패해도 폰 저장은
  그대로이고 "올릴 것" 표시('pressure_test_records_v1_sync')가 남아 다음에 다시 올린다.
- 지우기는 기록 내용을 뺀 지움 표시(`deleted: true`)만 남긴다(다른 폰에서도 지워지게).
- 목록을 열면 서버의 내 기록(`owner` = 내 이름)을 받아 합친다. 같은 id면 `editedAt`이 큰 쪽이 이긴다(같으면 폰 것).
  처음 한 번은 예전에 폰에만 저장한 기록을 모두 올리고 'pressure_test_records_v1_sync_uploaded'를 켠다.
- 목록 위 한 줄: "서버에 저장되어 있습니다…" / "폰에만 저장된 것 N건 · 통신되면 서버로 올라갑니다." / 로그인하지 않았으면
  "폰에만 저장됩니다. 로그인하면 서버에도 저장됩니다."
- 서버 규칙(firestore.rules)은 고치지 않았다. 이 모음은 ownedColls·specialColls에 없어 "로그인한 사람이면 읽고 쓴다" 칸에 든다.
- 검증: test/record_sync_test.dart(가짜 서버). 실제 서버·두 폰 사이 합치기는 폰에서 확인하지 않았다.

**기록**(`test_record.dart`, 폰에 먼저 저장 SharedPreferences 'pressure_test_records_v1', JSON 목록, 최근 것이 앞, 서버에도 올림):
- 칸: id, 시험일, 시험 번호, 현장·프로젝트, 계통, 라인 번호(꼭), P&ID·아이소 번호, 시험 구간(From ~ To), 규격, 시험 종류,
  시험유체(물·공기·질소·기타), 설계압력·시험압력(kPa로 저장), 압력 단위(보이는 단위), 규정 유지시간, 시작·종료 시각,
  측정 기록(시각·kPa·°C·구분), 허용 압력강하, 누설 확인, 압력계 최대 2개(번호·눈금 범위·검교정 유효일), 안전밸브
  설정압력·번호, 시험자, 입회자(시공사·감리·발주처), 메모, 관 외경·두께·재질. 결과는 저장하지 않고 읽을 때 계산한다.
- 칸이 빠지거나 망가져도 읽는다(없는 칸은 기본값, 망가진 측정 줄·기록 한 건은 건너뜀).
- 저장 창: 라인 번호가 없으면 막는다. 시험자는 마지막에 쓴 이름, 없으면 앱 사용자 이름('user_real_name').
  압력계·안전밸브는 마지막 저장 값('pressure_test_last_gear')을 미리 채운다('pressure_test_last_tester'도 같이).
  불러온 기록의 라인 번호를 바꾸면 "새로 저장"이 기본, 그대로면 "고쳐 저장"이 기본. 시험유체 기본값: 수압 물, 공압 공기.
- 저장한 기록 목록: 라인 번호·시험 번호·계통·날짜·시험자·규격과 시험 종류·시험유체·판정. 누르면 기록서 보기 ·
  계산기로 불러오기(시험 압력 탭의 규격·시험 종류·단위·설계압력·시험압력까지 되살림, 단위가 다르면 다른 탭 값도 환산) ·
  지우기(확인 창). CSV 내보내기(UTF-8 BOM, 한 기록 한 줄, 압력은 기록의 단위, 마지막 칸에 측정 기록 요약).

**기록서 PDF**(`test_record_pdf.dart`): 제목 "배관 압력시험 기록서"(KOLAS 성적서가 아니라 현장 시험 기록이라 "기록서"),
A4 한 장. 머리(시험 번호·시험일) → 시험 정보 → 압력계·안전밸브 표 → 측정 기록 표(구분·시각·경과(분)·압력·온도) →
판정 상자(측정 압력강하·온도 보정 후·허용 압력강하·유지시간 달성·누설 확인·최종 판정과 이유, 수압 물 온도 참고) → 메모 →
서명 칸(시험자·시공사 입회·감리 입회·발주처 입회). 압력은 기록의 단위. 파일 이름 pt_{라인 번호}_{yyyymmdd}.pdf
(파일 이름에 못 쓰는 글자는 _). 미리보기로 먼저 보이고, 공유는 미리보기의 "공유하기"를 눌러야만 된다.

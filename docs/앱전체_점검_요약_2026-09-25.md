# 앱 전체 점검 요약 — 제안 목록 (2026-09-25)

제안을 모은 목록입니다. 고른 묶음부터 진행하고, 끝난 것은 맨 아래 "진행 상황"에 적습니다.

## 어떻게 봤나
- **자동 검사**: `flutter analyze` 오류 0(안내 2개는 예전 것), `flutter test` 1,696개 모두 통과, GitHub 자동 검사 통과.
- **죽는 모양 훑기**: `firstWhere`에 `orElse` 없는 곳 4곳, `data()!` 1곳 — 모두 안전(값이 자기 목록에서만 오거나 먼저 확인함).
- **영역별 점검**(코드만 읽음, 현업 도구와 비교, 개인+공용 전제). 중요한 주장은 코드에서 다시 확인했다.

| 영역 | 보고서 |
|---|---|
| 작업일지·주간보고·펀치·사진 | `작업일지_현업도구_비교_2026-09-24.md` |
| 튜브·형강 컷팅 | `컷팅_현업도구_비교_2026-09-24.md` |
| 벤딩 계산기 화면·흐름 | `벤딩계산기_화면_현업도구_비교_2026-09-24.md` |
| 앱 뼈대·설정·알림·권한 | `앱뼈대_설정_알림_점검_2026-09-24.md` |
| (앞서 한 것) 배치도·자재·프로필·리모컨·계산기 셈 | 같은 폴더의 각 보고서 |

크기: **작음** = 몇 줄~한 파일, **중간** = 여러 파일·테스트 추가, **큼** = 저장 구조를 바꾸거나 화면을 새로.

## 1순위 — 숫자·자료가 틀리거나 사라짐

| # | 문제 | 어디 | 크기 |
|---|---|---|---|
| 1 | **지운 일정·단계·PC 일지가 다음 저장 때 되살아남.** 지운 표시 함수(`markItemDeleted`)를 아무도 안 부름. 혼자 써도 생김 | 작업일지 3절 1 | 작음 |
| 2 | **튜브 설정을 inch로 바꾸면 반경·게인 등이 25.4로 나뉘어 저장**되는데 칸 이름은 [mm], 엔진도 mm로 읽음 → R 38.1이 1.5로 쓰임 | 벤딩 화면 | 작음~중간 |
| 3 | **폰 설정 탭에서 튜브 외경이 3.0mm로 저장될 수 있음**(mm 목록에 12.7이 없어 첫 값으로). 태블릿 설정은 이미 고쳐져 있음 | 벤딩 화면 | 작음 |
| 4 | **튜브 재고 차감이 재단 계획과 다름**(길이 합 ÷ 한 본 올림: 3500×3개 = 계획 3본인데 2본 차감, 잔재에서 자른 것도 새 본으로 차감) | 컷팅 3-1 1 | 중간 |
| 5 | **PC에서 폰 일지를 고치면 칸 대부분이 지워지고 일지가 둘로**, 잠긴 일지도 까닭 없이 고쳐짐 | 작업일지 | 작음~중간 |
| 6 | **사진 정리가 새 주소 저장 전에 옛 파일을 지울 수 있음** → 사진이 영영 깨짐 | 작업일지 | 중간 |
| 7 | **태블릿 전기 마킹이 끝 피팅 깊이를 한 번 더 더함**(폰과 마지막 마킹이 다름) | 벤딩 화면 | 작음 |
| 8 | 컷팅 기록을 지워도 "재고에서 뺄 양"은 남음 → 나중에 지운 것까지 차감 | 컷팅 3-1 2 | 작음 |
| 9 | 잔재 목록을 통째로 덮어씀 → 다른 폰(또는 형강 화면)에서 바꾼 잔재가 사라짐 | 컷팅 3-1 6 | 중간 |
| 10 | 계산기 설정 서버 보관이 통째 덮어쓰기 → 새 폰에서 컷팅만 써 보고 로그인하면 벤딩 제원이 지워짐. 받은 설정이 화면에 안 들어가 다음 저장에 덮임 | 앱 뼈대 2-2 | 작음 |
| 11 | 도면 저장에 크기(늘 1/2")·반경·게인이 안 남음 → 다시 열면 지금 설정으로 셈, 자를 길이는 옛 값 | 벤딩 화면 | 중간 |
| 12 | 다른 폰에서 같은 일지·이슈를 그사이 고쳤으면 내 수정이 말없이 안 저장됨 | 작업일지 | 작음 |
| 13 | 형강 차감: 일부만 빠져도 "뺐음", 본 길이·단위(m) 무시, "뺐음" 표시가 폰에만 | 컷팅 3-1 3~5 | 작음~중간 |
| 14 | 톱날 두께 설정 하나를 튜브·형강이 같이 씀 | 컷팅 3-1 8 | 작음 |
| 15 | "앞 마킹에서 +N" 간격을 따로 반올림 → 간격대로 재면 굽힘마다 최대 0.5mm씩 밀림 | 벤딩 화면 | 작음 |
| 16 | 튜브 저장: 통신 없이 앱을 닫으면 합계는 올라가고 기록은 없음 | 컷팅 3-2 9 | 작음 |

## 2순위 — 통신 없을 때·알림이 끊김·작업을 잃음

| # | 문제 | 어디 | 크기 |
|---|---|---|---|
| 17 | 평일·격주 반복 일정 알림은 "내 일정" 화면을 열어야 다음 회차가 잡힘 | 앱 뼈대 2-3 5 | 작음 |
| 18 | 뒤로 한 번에 쓰던 것을 잃음: 기존 일지 수정, 새 이슈, PC 설정 | 작업일지·앱 뼈대 | 작음 |
| 19 | 통신 없으면 안 끝나는 저장: 일지 양식, 단계 템플릿, 주소록 삭제, 알림 시간, 형강 작업 삭제 | 작업일지·컷팅 | 작음 |
| 20 | 사진 들어간 PDF가 통신 없을 때 오래 멈춤(사진마다 시간 제한 없음, 최대 48장) | 작업일지 | 작음 |
| 21 | PC 시작 화면 "DB 초기화" 단추: 한 번 누르면 확인 없이 서버 부속 목록을 지우고, 통신 없으면 빠져나올 수 없음 | 앱 뼈대 2-4 10 | 작음(빼기) |
| 22 | 튜브 재단 계획 창이 캐시 없는 폰에서 통신 없으면 안 열릴 수 있음 | 컷팅 3-2 10 | 작음 |
| 23 | 도면(튜브 DB·전선관)이 어떤 백업에도 안 들어감 → 앱을 지우거나 폰을 바꾸면 사라짐 | 벤딩 화면 | 중간 |
| 24 | PC가 윈도우 앱이면 알림 준비에서 막혀 앱이 안 켜질 수 있음(확인 필요) | 앱 뼈대 2-1 1 | 작음 |

## 3순위 — 공유·주인

| # | 문제 | 어디 | 크기 |
|---|---|---|---|
| 25 | 프로젝트·일지를 모두가 읽고 쓰고 지움. 일지 양식·주소록·단계 템플릿은 모두가 한 문서를 같이 씀 | 작업일지 | 중간 |
| 26 | 잔재가 앱 전체에 문서 하나, 컷팅 작업·템플릿에 주인 없음 | 컷팅 3-4 | 중간~큼 |
| 27 | 서버 알림(일정·이슈)이 모든 사람 폰으로 감 | 앱 뼈대 2-3 9 | 중간 |
| 28 | PC 사진과 폰 사진이 서로 안 보임(PC 사진은 PC 파일 경로만 저장) | 작업일지 | 중간 |
| 29 | 나눠 줄 때: 디버그 키 서명·앱 id `com.example`·버전 늘 1.0.0+1 → 다른 컴퓨터 빌드는 업데이트가 안 되고 지우고 다시 깔아야 함 | 앱 뼈대 2-5 17 | 작음 |

## 4순위 — 정리·편의

| # | 제안 | 크기 |
|---|---|---|
| 30 | 숨긴 차량 기능이 홈에서 계속 서버를 들음, 지운 발주·채팅 서버 함수가 15분마다 돔 → 끊기 | 작음 |
| 31 | 안 쓰는 패키지(`google_fonts`·`cupertino_icons`)·저장소 권한 빼기 | 작음 |
| 32 | 아무 데도 안 쓰이는 설정(마커 정렬·마킹선 두께·기본 회전·기록 자동 저장·진동) 정리 또는 연결 | 작음~중간 |
| 33 | 현장 탭을 한 번 열면 앱 전체가 세로로 고정됨(나갈 때 안 풀림) | 작음 |
| 34 | 알림을 누르면 그 항목 열기, 서버 푸시 알림 채널, 권한은 쓰는 순간에 묻기 | 작음~중간 |
| 35 | 폰·PC 홈 메뉴 맞추기(PC에 프로필·현장 자료·전선관 없음), 상태 표시줄 되살리기 | 중간 |
| 36 | 일지 날짜 고르기(빠진 날 채우기), 달력 연도 구분 | 작음 |
| 37 | 컷팅: 조각 이름표(PT1→PT2), 가진 원자재 수량, 끝 다듬기·최소 잔재 설정, CSV | 중간 |
| 38 | 새로 저장한 도면이 보관함에 바로 안 보임, 3D가 옛 시작 방향을 들고 있음, 새들 되돌리기 3~4번 | 작음 |

## 앞서 미뤄 둔 것(할일 메모)
- 익명 로그인 콘솔 확인 → `firestore.rules` 올리기
- 수평계·각도기·리모컨·재고 화면 폰 확인
- 자재: 같은 이름 다른 제조사 차감 때 묻기, CSV, "발주 필요" 하나로
- 배치도: DXF 내보내기, 튜브 경로·벤딩 반경, 동시 수정 경고
- 리모컨: 오프셋·새들 여러 줄로, 이력 서버에서 불러오기

## 진행 상황
- **묶음 A 끝(2026-09-25)**: 1·3·7·8·12·14·15·16·17·21번. 모두 테스트를 붙였고(17·21번은 연결만이라 제외) 전체 1,711개 통과. 커밋마다 번호를 적었다.
  - 8번은 튜브만 뺀다(부속은 두 구간 사이에 같이 쓰여 기록 몫을 나눌 수 없음).
  - 14번: 형강은 따로 기억하고 처음 한 번 예전 값을 이어받는다. 예전에 형강에 맞춰 넣은 값이 튜브에도 남아 있으니, 튜브 컷팅에서 톱날 손실을 한 번 확인할 것.
  - 21번: PC 시작 화면 단추만 뺐다. 폰 튜브 컷팅 목록의 "부속 DB 새로고침"(확인 창 있음)은 남기고 통신 확인을 더했다.
- **묶음 B 끝(2026-09-25)**: 2·10·11번. 모두 테스트를 붙였고, 예전 코드에서는 그 테스트가 실패하는 것을 확인했다. 전체 1,718개 통과.
  - 2번: 단위를 바꿔도 반경·게인 등 장비 값은 늘 mm로 둔다(칸 이름·엔진이 mm). 벽 두께만 바꾼다. **이미 망가진 값은 고치지 않는다.** 예전에 inch로 바꾼 적이 있으면 반경·게인·피팅 깊이를 한 번 확인할 것.
  - 10번: 서버에는 칸별로 합쳐 올리고, 받을 때는 폰에 없는 칸만 채운다("서버에서 불러오기" 단추는 전부 바꿈). 받은 뒤 설정을 다시 읽는다. 하는 김에, 설정을 읽기만 해도 기본값 0을 폰에 적던 것을 막았다(그 때문에 불러오기가 "이미 있다"며 건너뛸 수 있었다).
  - 11번: 새로 저장하는 도면은 설정의 관 크기를 먼저 고르고, 반경·게인·스프링백·벤더 원점·피팅 깊이를 같이 남긴다. 다시 열면 그 값으로 마킹을 셈하고, 지금 설정과 다르면 노란 글로 알린다("계산기로 불러오기" 창에도). 예전에 저장한 도면은 값이 없어 전처럼 지금 설정으로 셈한다.
- **묶음 C 끝(2026-09-25)**: 4·9·13번. 모두 테스트를 붙였다. 전체 1,734개 통과.
  - 4번: **튜브 재고는 이제 형강처럼 재단 계획 창의 "재고에서 빼기"로 뺀다.** 새 원자재 본수만큼 빠지고(3500×3 = 3본), 잔재에서 자른 것은 안 빠진다. 결과 탭 "저장하기"는 튜브 길이를 더 쌓지 않는다(부속만). 목록의 "재고 차감"은 부속과 예전에 쌓인 튜브 길이를 뺀다. 저장할 때 튜브를 아직 안 뺐으면 알림에 한 줄 적는다.
  - 9번: 잔재마다 이름표를 붙이고 쓴 것만 빼고 새 것만 더한다(되돌리기·잔재 관리·재고 화면 지우기도). 예전 잔재는 처음 읽을 때 이름표를 붙여 다시 적는다.
  - 13번: 이미 뺀 본을 적어 두고 남은 본만 뺀다. 못 뺀 규격은 "남은 N본 재고에서 빼기"로 다시 뺄 수 있다. 창고가 m로 세면 본 길이를 더해 m로 뺀다. "뺐음" 기록은 작업 문서(서버)에 적는다. 튜브는 입력 줄마다라 폰의 임시 저장에 적는다.
- **묶음 D 끝(2026-09-25)**: 5·6·18·19·20번. 전체 1,746개 통과. 이로써 1·2순위(1~24번) 가운데 22·23·24번 말고는 모두 고쳤다.
  - 5번: PC 일지 고치기는 원래 일지 위에 바꾼 칸만 덮는다(아이디·작성자·인원·연결 이슈·확정 기록 유지). 확정된 일지는 PC에서도 해제 사유를 받는다.
  - 6번: 저장소가 "아직 서버에 안 닿은 저장" 수를 서버 읽기 전에 올려, 사진 정리가 새 주소 저장 전에 옛 파일을 지우지 않는다. 새 주소로 바꾼 일지·이슈에 시각을 찍어 다른 폰의 옛 주소가 이기지 않는다. (저장소 순서 자체는 서버 없이 시험할 수 없어 합치기 쪽만 테스트했다.)
  - 18번: 지난 일지 고치기·새 이슈 등록·PC 설정에서 바뀐 것이 있으면 뒤로 가기 전에 묻는다.
  - 19번: 보고서 양식·단계 템플릿·주소록·자재 즐겨찾기는 서버 쓰기를 기다리지 않는다. 알림 시각 바꾸기는 알림부터 다시 잡는다. 형강 작업 지우기는 통신이 없으면 폰 캐시로.
  - 20번: PDF 사진 한 장 10초까지만 기다리고, 넘기면 1분 동안 나머지 서버 사진은 건너뛴다(사진 없이 나옴). 주간 보고 PDF 단추는 만드는 동안 "만드는 중…"으로 막는다.
- **22·23·24번 끝(2026-09-25)**: 전체 1,753개 통과. 1·2순위(1~24번)를 모두 고쳤다.
  - 22번: 잔재를 못 읽으면(새로 깐 폰 + 통신 없음) 재단 계획 창이 안 열리던 것 → 잔재 없이 새 원자재로만 계산하고 알린다. 잔재 읽기는 8초 뒤 폰 캐시로.
  - 23번: 백업 파일(내보내기·주간 자동 백업)에 튜브·전선관 도면 보관함을 같이 넣는다. 되돌릴 때는 없는 것만 더한다. 프로젝트가 없어도 도면이 있으면 자동 백업.
  - 24번: 알림 준비에 윈도우·리눅스 설정을 넣고, 알림 준비가 실패하거나 8초를 넘겨도 앱은 켜진다. 서버 알림(FCM)은 안드로이드·iOS·macOS에서만 준비한다(윈도우는 지원하지 않음). **윈도우에서 실제로 빌드해 켜 보지는 못했다.**

## 추천 묶음
- **묶음 A(작은 것 모아 한 번에, 숫자·자료 먼저)**: 1, 3, 7, 8, 12, 14, 15, 16, 17, 21 — 대부분 몇 줄씩. 테스트를 붙여 한 번에 올리기 좋다.
- **묶음 B(설정 단위·보관)**: 2, 10, 11 — inch 전환과 설정 보관을 같이 손보기.
- **묶음 C(컷팅 재고·잔재)**: 4, 9, 13 — 튜브·형강 차감을 재단 계획 한 길로, 잔재를 한 개씩.
- **묶음 D(일지 합치기)**: 5, 6, 18, 19, 20.
- 공유(3순위)는 실제로 나눠 쓰기 시작할 때.

